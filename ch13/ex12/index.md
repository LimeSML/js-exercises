# 予想

1秒後には `"Hello, world!"` は表示されず、しばらくたってから表示される

# 理由

このコードでは `longA()` と `longB()` が無限ループし毎回 `await Promise.resolve()` を実行するためマイクロタスクが絶えず発生し、イベントループがタスク（`setTimeout()`）に進めず `"Hello, world!"` は実行されず `"A"` と `"B"` だけが延々と出力される。

# 実際の結果

出力されなかった
